{% import '_includes/forms' as forms %}

{% set categories = element.groups %}

{{ forms.selectField({
    label: "Category Group" | t('export'),
    instructions: 'Choose the category group you want to export.' | t('export'),
    id: 'elementGroup-' ~ elementType,
    name: 'elementGroup[' ~ elementType ~ ']',
    options: craft.export.getSelectOptions(categories),
    value: export.elementGroup[elementType] ?? '',
    required: true,
    errors: export.getErrors('elementGroup[' ~ elementType ~ ']')
}) }}
